<%if (administrator_signed_in?) then %>
<nav class="navbar navbar-dark bg-dark">

  <a class="navbar-brand" href="#">Administrador</a>
  <h4 class="text-light bg-dark">Reservas <span class="sr-only">(current)</span></h4> <!-- Aqui seccion actual -->

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/">Inicio </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/residences">Propiedades </a>  <!-- Aqui seccion actual -->
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/buscador">Buscar</a>
      </li>
      <li class="nav-item active">
       <a class="nav-link" href="/reservations">Reservas <span class="sr-only">(current)</span></a>
     </li>
     <li class="nav-item">
       <a class="nav-link" href="/clients">Clientes</a>
     </li>
     <li class="nav-item">
       <a class="nav-link" href="/ayuda">Ayuda</a>
     </li>
   </ul>
 </div>
</nav>
<br>

<hr> 
<% Reservation.all.order('fecha DESC').each do |reservation|%>
<button class= " btn btn-outline-info btn-lg btn-block "><%="#{ Client.find(reservation.client_id).apellido}, #{Client.find(reservation.client_id).nombre}.#{reservation.fecha},#{reservation.fechaInicio}, Precio:  #{reservation.precio}, Residencia: #{Residence.find(reservation.residence_id).nombre}"%></button>
      <hr>

      <%end%> 
    </div>
    <%end%>